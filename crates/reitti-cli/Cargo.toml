# Thin binary crate over `reitti-core` (§22).
[package]
name = "reitti-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "reitti"
path = "src/main.rs"

[dependencies]
reitti-core = { path = "../reitti-core" }
