---
schema_version: 1
status: approved
maturity: mvp
ecosystems: [rust]
targets:
  - {ecosystem: rust, package: reitti-cli, registry: gh-releases, adapter: cargo-dist}
distribution:
  adapter: cargo-dist
  gh_releases: true
  installers: [shell]
  platforms:
    - aarch64-apple-darwin
    - aarch64-unknown-linux-musl
    - x86_64-unknown-linux-musl
versioning: semver
changelog: {mode: curated, source: issuectl-trailers}
conventional_commits: false
release: {model: gated, layout: single}
contribution_provenance: none
provenance_level: keyless
dependency_bot: none
health_badges: [license]
license: MIT
docs_site: none
---

> **APPROVED UNDER DELEGATED AUTHORITY.** Generated by `/shipshape-init` on 2026-09-08 from "Agent-first command-line journey planner powered by Digitransit open data." The maintainer expressly delegated routine release-design choices for this preparation run; this records that delegation and does not claim a separate human review.

## Rationale

- **Maturity: MVP** — `shipshape facts` infers `spike` from one committer, no CI, and no tags, but the maintainer expressly delegated preparation for a distributable v1.0. MVP enables proportionate release and contributor foundations without claiming that the placeholder CLI is release-ready.
- **Ecosystem: Rust** — the workspace contains `reitti-core` and the `reitti` binary package (`reitti-cli`).
- **Release target: `reitti-cli` on GitHub Releases** — no crates.io package is published. Source installation remains possible from the repository, while cargo-dist owns the future binary release target.
- **Distribution: cargo-dist via GitHub Releases** — future releases provide binaries for macOS arm64 and Linux arm64/x86_64 plus a shell installer. Intel macOS and Windows are intentionally unsupported.
- **Versioning and release: SemVer, gated, single** — the first intended stable release is v1.0. A maintainer explicitly initiates every release; this contract does not authorize automatic publication.
- **Changelog: curated from issuectl trailers** — maintainers select user-facing entries from the repository's issue-linked commits.
- **Provenance: keyless** — future GitHub release automation should attach keyless build provenance. No provenance is claimed for the current placeholder or local builds.
- **License: MIT** — the workspace manifest declares MIT, matching the planned public license in `README.md`.
- **Documentation and health signals** — no separate docs site is planned, and only the license badge is enabled until CI exists. Dependency automation is deferred to the release integration slice.

## Release notes

- The repository remains private until the maintainer changes visibility. This contract does not change GitHub settings.
- Do not create tags, GitHub releases, or public registry entries while preparing these foundations.
- A future v1.0 cut must first add and verify cargo-dist configuration, CI, changelog/release notes, clean-machine installation, and the completed CLI. Release integration must also remove the temporary private/placeholder wording from the contract and onboarding/security documents once those statements no longer apply.
- GitHub Release assets and pushed tags are public, durable release actions once repository visibility changes; review the release plan before cutting them.
- Transit and map data keep their upstream licenses and attribution requirements; the MIT license covers this repository's software, not third-party data.
