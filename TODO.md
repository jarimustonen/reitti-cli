# Work-session handoff

## 🔄 Continue here

The v1.0 journey-planner implementation and public release are complete. The repository is public, and **v1.0.0** is available on [GitHub Releases](https://github.com/jarimustonen/reitti-cli/releases/tag/v1.0.0), crates.io (`reitti-core` and `reitti-cli`), and the [Homebrew tap](https://github.com/jarimustonen/homebrew-reitti). The installed command is `reitti`; installation uses `cargo install --locked reitti-cli` or `brew install jarimustonen/reitti/reitti-cli`. The release tag points to `2aeb5e1be33a29de08d805f40fd266c6f73f731c`; later documentation commits do not change the published binaries' source identity.

The CLI provides location resolution, nearby stops, departure/arrival journey planning, departure boards, service alerts, secure configuration, diagnostics, structured help and schemas, and a version-matched Agent Skill for Claude Code, Pi and Codex. Complex travel decisions remain the calling agent's responsibility. Start with the README, `reitti journey list --help`, `docs/configuration.md`, and the bundled skill when investigating behavior. Read the immutable CLI canon before changing the command surface.

Final evidence is in [release acceptance](issues/release-v1/acceptance.md). All 77 Rust tests, 21 provider fixtures and local repository gates passed. Actual crates.io and isolated Homebrew installations each passed 35 checks, as did public shell installation on macOS ARM64 and both Linux architectures (x86_64 exercised under explicit emulation). All three published binary attestations verified. [GitHub release run 34242521413](https://github.com/jarimustonen/reitti-cli/actions/runs/34242521413) passed every job, using the required self-hosted macOS runner. Private Vulnerability Reporting is enabled. Shipshape run `01M20RDJQMVH8Y54AVZ02ZZH8B` is completed.

Preserve the maintainer's decisions: crates.io and Homebrew are the first installation channels; ordinary push/PR CI and Code of Conduct are intentionally absent. The generated tag-triggered cargo-dist workflow remains. Keep the macOS runner override, build-setup path remapping, and delegated Homebrew publication settings when regenerating distribution files; see [distribution operations](docs/DISTRIBUTION.md). Do not reintroduce removed features merely to satisfy an audit. Judge review findings against actual source and behavior, and reject speculative churn. Keep credentials out of tracked files, arguments, logs and worker copies; the configuration guide covers safe setup.

All implementation workers have settled and their work has landed. Historical cancelled run `01m2015e5pq98215wdvw8j0k5e` still has its preserved recovery worktree; completed continuation `01m207d6rd3y4hp3ga94pk2s55` adopted the complete recovery patch and landed it, as recorded in [foundation implementation](issues/scaffold-rust-workspace/implementation.md). That copy is historical recovery material, not unfinished implementation or an active worker; do not delete it or treat it as a fresh task automatically. Some ignored local validation caches reference it.

The maintainer subsequently approved a GitHub About description beginning “The familiar Reittiopas experience, now for your AI agent”; the repository setting was updated and verified.

No product decision or requested implementation remains unresolved from this stint. The implementation epic and release issue are closed. Future work should start from a new maintainer request and the live `issuectl dag --json`, not the superseded first-stint plan that this narrative replaces. Digitransit's unpublished numeric quota remains an intentional documented limit, not a new task. No test-account reset is configured for this CLI project.
